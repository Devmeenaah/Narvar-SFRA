<isif condition="${'deliveryPromiseEnabled' in dw.system.Site.current.preferences.custom 
                    && dw.system.Site.current.preferences.custom.deliveryPromiseEnabled == true 
                    && 'enableNarvarPromiseInCheckout' in dw.system.Site.current.preferences.custom 
                    && dw.system.Site.current.preferences.custom.enableNarvarPromiseInCheckout == true
                    && pdict.product.available }">
    <div class="shipping-method-delivery-date js-delivery-date-display" 
         data-method-id="${shippingMethod.ID}"
         style="display: none;">
    </div>
</isif>